# Make sure you are on your custom branch
git checkout tweaks

# Fetch the latest code from the original author
git fetch upstream

# Merge the changes from their `main` branch into yours
git merge upstream/main